id: "00000000-0000-0000-0000-000000000005"
name: "deny_events"
display_name: "Deny Events"
description: "Never fires; routed through the deny-all matcher for load testing. Use for testing only."
enabled: true
version: "1.0.0"

severity: "low"
confidence: "low"

log_types: ["denied"]
matchers: ["deny-all"]

req_subkeys: []
merge_by_keys: ["source_ip"]
merge_window_mins: 5

signal: false
tags: ["testing"]
